{
  "name": "witcha-chimphlee-archive",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-images.mjs",
    "dev": "npm run predev && vite --host",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@vladmandic/face-api": "^1.7.15"
  }
}
